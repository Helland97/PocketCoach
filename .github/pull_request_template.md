## What

<!-- One or two sentences. What does this PR change? -->

## Why

<!-- The motivation. What problem does this solve, or what does it enable? -->

## How tested

<!-- How did you verify the change works? Examples:
     - Ran `docker-compose up --build`, uploaded a squat video, checked the verdict UI
     - `npm run lint` and `npm run build` clean
     - `dotnet build` clean
     - Manual: clicked through X, Y, Z
     - "Not tested — config-only change" is a valid answer, just say so
-->

## Notes

<!-- Optional: follow-ups, known limitations, things reviewers should look at twice. Delete this section if empty. -->
